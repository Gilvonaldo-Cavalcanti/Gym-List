export interface ConclusaoSemanal {
    id?: string,
    userId?: string,
    domingo?: boolean,
    segunda?: boolean,
    terca?: boolean,
    quarta?: boolean,
    quinta?: boolean,
    sexta?: boolean,
    sabado?: boolean,
}
